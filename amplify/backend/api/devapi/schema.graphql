type Comment
  @model
  @auth(rules: [
      {allow: public, operations: [read]},
      {allow: owner, operations: [create, update, delete]}
    ])
{
  id: ID!
  content: String!
  owner: String!
  postPath: String!
  timestamp: AWSDateTime!
}
